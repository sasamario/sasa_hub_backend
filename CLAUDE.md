# CLAUDE.md

このファイルは Claude Code がセッション開始時に読み込む、本リポジトリの前提と方針をまとめたものです。
作業を始める前に必ず内容を踏まえてください。

## このリポジトリについて

自己学習の成果物(GitHub / Qiita の活動)を可視化する個人用ツールの **バックエンド** リポジトリです。
フロントエンドは別リポジトリで管理しています(`sasa_hub_frontend`。設計の正本もそちら側にあります。後述)。

- 目的1(実用): 自己学習の成果物を継続的に記録・可視化し、定量的に振り返る
- 目的2(学習): AIを活用してフロント/バックを一から開発し、技術理解を深める

**重要**: 本プロジェクトは学習目的を含みます。単に動くものを速く作ることより、
利用者(オーナー)が理解しながら進められることを優先してください(下記「進め方の方針」参照)。

## 技術スタック(バックエンド)

- 言語: TypeScript
- フレームワーク: NestJS
- データベース: PostgreSQL
- ORM: Prisma
- フロント連携: REST API(本リポジトリがAPIを提供する)
- 開発環境: Docker

## 進め方の方針(最重要・フロント側と共通)

オーナーは「教わりながら自分でも手を動かす」ことを望んでいます。以下を必ず守ってください。

1. **一度に大量のコードを書かない。** いきなり全実装を提示せず、小さな単位に分けて進める。
   まず「何を・なぜ・どう作るか」の方針を説明し、オーナーと合意してから実装に入る。
2. **実装前に相談する。** 新しい機能やファイルに着手するときは、先に方針(設計・使うライブラリ・
   ファイル構成の案)を示し、確認を取ってからコードを書く。
3. **理解を助ける説明を添える。** コードを出すときは、何をしているかを日本語で説明する。
   オーナーが写経・改変しながら理解できる粒度を意識する。
4. **作法をドキュメント化する(下記「開発手順の記録」)。**

## コミュニケーション

- **日本語で応答する。**
- コード内のコメントは、学習目的のため要点にはある程度丁寧に付ける
  (自明な行に過剰なコメントは不要。なぜそうするかが分かる程度)。

## 開発手順の記録(作法のドキュメント化)

オーナーは「後で振り返ったときに、この作業はこう進めればよいと分かる状態」を望んでいます。

- **新しい種類の作業を初めて行ったら、その手順を `docs/guides/` に残す。**
  例: 新しいエンドポイントを追加する手順、Prismaでテーブルを追加する手順、
  外部API連携(GitHub/Qiita)を追加する手順 など。
- ファイルは作業の種類ごとに分ける(例: `docs/guides/add-endpoint.md`, `docs/guides/add-table.md`)。
- 手順書には「用意するファイル」「実行するコマンド」「注意点」を具体的に書く。
- **上記に加えて、分野ごとの汎用知識は `docs/guides/<分野名>/` にまとめる。**
  例: `docs/guides/nestjs/`, `docs/guides/prisma/`, `docs/guides/docker/`。
  - 作業手順書(`docs/guides/` 直下)は「その作業をどう進めるか」の時系列的な記録、
    分野別フォルダは「その分野について知っておくべきこと(汎用コマンド・概念など)」の蓄積、
    という役割分担。内容が重複しても問題ない。
- 作業を進める中で該当する手順書がまだ無い場合は、作成をオーナーに促す。

### 学びの記録を促す(まとめ漏れ防止)

オーナーは初めて Claude / Claude Code を使うため、進めながら得た知見を資産として残したい
と考えています。**まとめた方がよさそうな内容に気づいたら、勝手に書かず「まとめますか?」と
確認を取ってください。** 記録先は内容に応じて振り分けます。

- **このプロジェクト固有の作業手順**(例: テーブル追加手順、エンドポイント追加手順)→ `docs/guides/`
- **Claude / Claude Code の使い方や開発の進め方など、他プロジェクトでも役立つ汎用知識**
  → `claude-guides/`(リポジトリ直下。docsとは別。プロジェクト横断で使える想定)

判断に迷う場合もオーナーに確認する。まとめ漏れを防ぐことを優先する。

## 設計ドキュメントの参照(フロントリポジトリ側が正本)

設計の正本は `sasa_hub_frontend` リポジトリにある。ローカルでは兄弟ディレクトリに
配置している前提で、相対パスで直接参照する。

- 要件定義: `../sasa_hub_frontend/docs/requirements.md`
- 基本設計(データモデル / API / 画面 / 同期処理): `../sasa_hub_frontend/docs/architecture.md`
- 設計判断の記録(ADR): `../sasa_hub_frontend/docs/adr/`

バックエンド実装で主に参照するのは以下(architecture.md の該当章)。

- **データモデル**(4章): テーブル定義・一意キー・同期時の扱い(追加のみ/upsert)
- **REST API設計**(6章): エンドポイント一覧・パラメータ・カーソル方式ページング
- **同期処理設計**(7章): 差分取得・トランザクション方針・エラー時の継続方針

### フロントとの認識合わせが必要な事項(要注意)

以下はフロント側の実装が先行して進む中で判明した、architecture.md からの変更・未確定事項。
バックエンド実装時に矛盾がないか都度確認し、確定したら architecture.md 側に反映を提案する
(architecture.md の更新はフロントリポジトリ側で行うため、オーナー経由で反映する)。

- **期間プリセット変更**: 「任意期間」は廃止。「直近1年 / 6ヶ月 / 3ヶ月 / 1ヶ月」の4つ(デフォルト1年)。
- **ダッシュボードの `/api/qiita/summary` 呼び出しは `from`/`to` を指定しない**(常に全期間)。
  パラメータ自体はAPIとして受け付けられるようにしておく(将来のQiita詳細画面用)。
- **`/api/sync` (一括同期) は画面からは使わない。** 開発用・スクリプト用として実装は残す。
- **APIレスポンスのJSON構造は未確定。** フロントのモック(`sasa_hub_frontend/app/mocks/`配下)が
  暫定形を仮定しているが、正式にはバックエンド実装時に両リポジトリですり合わせて決める。
  決まったら architecture.md 8章に確定情報として追記する(オーナー経由)。
  - 暫定形の例: `GET /api/github/summary` → `{ commitCount, prCount }` など。
    詳細はオーナーに確認するか、フロントのモック実装を参照する。
  - `cursor` はフロント側では中身を解釈せず、もらった文字列をそのまま次のリクエストに渡すだけ。
    バックエンド側で扱いやすい実装(例: `activity_date_id` 形式)にしてよい。
- **対象リポジトリ一覧**: 環境変数/設定ファイルで管理する方針(architecture.md 6.4参照)。
  フロントのモックで使った `sasa_tools` / `sasa_hub_frontend` / `sasa_hub_backend` /
  `public-learning-repository` はダミー例。実装時は実際の対象リポジトリ名に置き換える。

### 未確定・要すり合わせ事項(architecture.md 8章より、バックエンド関連分)

- 差分取得の起点が取れない初回同期時、どこまで遡って取得するか
- 未マージPRの一覧での見せ方
- 手動記録(manual_records)のテーブル設計(スコープ外の可能性あり。要確認)

## タスク管理

- 実装タスクは `docs/tasks.md` にチェックリストで管理する。
- タスクに着手・完了したら、該当項目のチェックを更新する。
- 大きなタスクは、進める前に小さなサブタスクに分解してから着手する。
